version: '3.8'
services:
  db-dev:
    image: postgres:13-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: nest_playground
    ports:
      - 5432:5432
    networks:
      - postgres-network
networks:
  postgres-network:
